{
  "name": "image-style-transfer-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://dariocostanzo.github.io/image-style-transfer-app",
  "dependencies": {
    "axios": "^1.6.2",
    "gh-pages": "^6.3.0",
    "image-style-transfer-app": "file:",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
